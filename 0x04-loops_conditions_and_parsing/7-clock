#!/usr/bin/env bash
# This script is displaying te hours every 60 minutes
i=0
j=1
while [ "$i" -le 12 ]
do
	echo "Hour: $i"
	if [ "$i" -lt 12 ]
	then
		j=1
		while [ "$j" -le 60 ]
		do
			echo "$j"
			((j++))
		done
	fi
	((i++))
done
